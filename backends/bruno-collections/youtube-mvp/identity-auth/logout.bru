meta {
  name: logout
  type: http
  seq: 3
}

post {
  url: {{BASE_URI}}/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
