apiVersion: v1
kind: Service
metadata:
  name: {{ include "moderation-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "moderation-service.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080


