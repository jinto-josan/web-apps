apiVersion: v1
kind: Service
metadata:
  name: {{ include "anti-abuse-service.fullname" . }}
  labels:
    {{- include "anti-abuse-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anti-abuse-service.selectorLabels" . | nindent 4 }}

