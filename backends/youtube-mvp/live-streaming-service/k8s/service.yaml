apiVersion: v1
kind: Service
metadata:
  name: live-streaming-service
  namespace: default
  labels:
    app: live-streaming-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: live-streaming-service

