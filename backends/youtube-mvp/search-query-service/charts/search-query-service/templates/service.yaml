apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-query-service.fullname" . }}
  labels:
    {{- include "search-query-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "search-query-service.selectorLabels" . | nindent 4 }}
