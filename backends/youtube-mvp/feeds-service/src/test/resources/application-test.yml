spring:
  profiles:
    active: test
  
  data:
    redis:
      host: localhost
      port: 6379
      ssl:
        enabled: false
  
  cloud:
    azure:
      appconfiguration:
        enabled: false
      keyvault:
        secret:
          enabled: false

management:
  metrics:
    export:
      azure-monitor:
        enabled: false

logging:
  level:
    root: WARN
    com.youtube.mvp.feeds: DEBUG

