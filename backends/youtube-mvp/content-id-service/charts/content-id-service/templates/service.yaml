apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-id-service.fullname" . }}
  labels:
    {{- include "content-id-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "content-id-service.selectorLabels" . | nindent 4 }}

