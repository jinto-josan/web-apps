apiVersion: v1
kind: Service
metadata:
  name: config-secrets-service
  namespace: youtube-mvp
  labels:
    app: config-secrets-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: config-secrets-service

