spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/configsecrets_test
    username: test
    password: test
  
  data:
    redis:
      host: localhost
      port: 6379
      password:
  
  cloud:
    azure:
      appconfiguration:
        enabled: false
      keyvault:
        secret:
          enabled: false
  
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test-issuer.com

logging:
  level:
    root: WARN
    com.youtube.configsecretsservice: DEBUG

